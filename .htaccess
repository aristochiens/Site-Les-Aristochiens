# -----------------------------
# Fichier .htaccess mis à jour
# - Forcer HTTPS
# - En-têtes de sécurité (HSTS, X-Frame, etc.)
# - Protection des fichiers sensibles
# - Compression et règles de cache
# - Réécritures pour URLs amicales
# -----------------------------

# Activer le moteur de réécriture d'URL
RewriteEngine On

# Forcer HTTPS (compatible avec proxy via X-Forwarded-Proto)
RewriteCond %{HTTPS} !=on
RewriteCond %{HTTP:X-Forwarded-Proto} !https
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

# Sécurité : en-têtes HTTP utiles
<IfModule mod_headers.c>
    Header always set Strict-Transport-Security "max-age=63072000; includeSubDomains; preload"
    # Removed X-Frame-Options to allow Google Maps embedding
    # Header set X-Frame-Options "SAMEORIGIN"
    Header set X-Content-Type-Options "nosniff"
    Header set X-XSS-Protection "1; mode=block"
    Header set Referrer-Policy "no-referrer-when-downgrade"
    # Content Security Policy to allow Google Maps and Fonts
    Header set Content-Security-Policy "frame-src 'self' https://www.google.com https://maps.googleapis.com; font-src 'self' https://fonts.googleapis.com https://fonts.gstatic.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data: https:; script-src 'self' 'unsafe-inline' https://unpkg.com;"
</IfModule>

# Bloquer l'accès aux fichiers cachés et sensibles
<FilesMatch "^\.">
    Require all denied
</FilesMatch>

<FilesMatch "\.(env|ini|phps|sql|bak|config|dist|json)$">
    Require all denied
</FilesMatch>

# Empêcher la navigation dans les répertoires
Options -Indexes

# Document d'erreur personnalisé (optionnel)
ErrorDocument 404 /404.html

# Compression (si disponible) pour améliorer les performances
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json image/svg+xml
</IfModule>

# Règles d'expiration (cache côté client)
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType text/javascript "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType application/x-javascript "access plus 1 month"
    ExpiresByType image/svg+xml "access plus 1 month"
    ExpiresByType image/jpg "access plus 1 month"
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType image/gif "access plus 1 month"
    ExpiresByType image/webp "access plus 1 month"
    ExpiresDefault "access plus 7 days"
</IfModule>

# Réécritures conviviales pour pages statiques
RewriteRule ^Accueil/?$ /index.html [L]
RewriteRule ^About/?$ /about.html [L]
RewriteRule ^Contact/?$ /contact.html [L]

# Page par défaut
DirectoryIndex index.html

